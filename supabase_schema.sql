-- =====================================================================
--  ARCHIVO NEUTRALIZADO  ⚠️ NO EJECUTAR
--
--  Este archivo creaba las tablas la primera vez que conectaste el CRM
--  con Supabase. Ya no es necesario — tus tablas ya existen.
--
--  Si lo ejecutaras de nuevo BORRARÍA todos tus datos
--  (clientes, proyectos, terrenos, gastos…) porque empieza con
--  DROP TABLE ... CASCADE.
--
--  Por eso lo he vaciado. Si en algún momento quieres recrear el
--  esquema desde cero (algo que NUNCA deberías hacer en producción),
--  pídemelo y te genero un archivo nuevo aparte.
-- =====================================================================

-- (sin operaciones — archivo intencionalmente vacío)
select 'Archivo neutralizado. No hace nada.' as info;
